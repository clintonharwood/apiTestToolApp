<%- include('partials/_head', { title: 'Create Account Result — Clintox' }) %>

<h1>Create Account Result</h1>

<div class="card mt-3">
  <div class="card-body">
    <pre><%= typeof result === 'string' ? result : JSON.stringify(result, null, 2) %></pre>
  </div>
</div>


<%- include('partials/_foot') %>
